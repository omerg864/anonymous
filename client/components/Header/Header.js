function openSidebar() {
    document.getElementById("mySidebar").style.width = "250px"; // Open the sidebar by setting its width
}
function closeSidebar() {
    document.getElementById("mySidebar").style.width = "0"; // Close the sidebar by setting its width to 0
}